package com.parking.lot.entities;

public class WeekdayAlgorithm extends PaymentAlgorithm {
    @Override
    public int calculatePayment(ParkingSpace parkingSpace, ParkingSlip parkingSlip) {
        return super.calculatePayment(parkingSpace, parkingSlip);
    }
}
